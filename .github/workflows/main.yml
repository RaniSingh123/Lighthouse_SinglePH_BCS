name: LighoutBCS

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - master

jobs:
  build-test-publish:
    name: CI pipeline
    runs-on: ubuntu-latest
    steps:
      - name: Buiding
        run: echo "Builder"
