name: LighoutBCS

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build-test-publish:
    name: CI pipeline
    runs-on: ubuntu-latest
    steps:
      - name: Setup SDK
        run: echo "SDK Runner"
      
      
      

  
